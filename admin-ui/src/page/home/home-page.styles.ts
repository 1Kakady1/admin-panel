import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

export const useStylesHomePage = makeStyles((theme: Theme) =>
    createStyles({
        root: {
            display: 'flex',
        },
    })
    
);
