import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

export const useStylesProdCreatePage = makeStyles((theme: Theme) =>
    createStyles({
        root: {
            display: 'flex',
        },
    })
    
);
